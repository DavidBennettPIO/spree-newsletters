%ul#module_list{:style => 'list-style: none outside none;'}
  - @newsletter.newsletter_lines.each do |nl|
    %li{ :id => 'module_' + nl.id.to_s, :class=> 'module_' + nl.module_name, :"data-module-id" => nl.module_id}
      = icon(nl.module_name)
      = nl.module_value